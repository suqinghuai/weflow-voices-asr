# WeFlow 语音转文字插件 - 使用教程

---

## 一、这个工具是干什么用的？

简单来说，这个工具可以帮您把微信聊天记录里的**语音消息**转换成**文字**，并且自动填回到聊天记录文件中。

这样您就可以：
- 查看语音消息的文字内容
- 方便地搜索聊天记录
- 把聊天记录导出成 PDF 保存

---

## 二、使用前的准备工作

### 第一步：导出微信聊天记录

您需要先使用 **WeFlow 4.3.1** 软件导出微信聊天记录，并且要确保：
1. ✅ 导出时选择了「原始语音文件」（wav格式）
2. ✅ 导出的文件夹里有 `media/voices` 这个目录
3. ✅ `voices` 文件夹里确实有 `.wav` 格式的语音文件

### 第二步：准备工具文件

您需要准备好以下文件：
- `Wav_to_txt.exe` （主程序）
- `config.ini` （配置文件）

> 📌 提示：如果您没有 `Wav_to_txt.exe`，可以联系开发者获取打包好的程序。

---

## 三、安装和使用（超级简单！）

### 方法一：单目录处理（推荐新手使用）

这是最简单的方法，适合处理单个聊天记录文件夹。

**步骤1：复制文件**
- 找到您导出的微信聊天记录文件夹（里面有很多 `.html` 文件）
- 把 `Wav_to_txt.exe` 和 `config.ini` 这两个文件**复制**到这个文件夹里

**步骤2：运行程序**
- 双击 `Wav_to_txt.exe` 文件
- 会弹出一个黑色的命令窗口

**步骤3：选择模式**
- 窗口里会显示两个选项：
请选择处理模式:

直接识别处理当前目录
手动填写总目录路径（批量处理）
请输入选项 (1/2):

- 输入数字 `1` 然后按回车键

**步骤4：等待处理**
- 程序会自动：
- 扫描语音文件
- 把语音转换成文字
- 自动填回到聊天记录里
- 您会看到很多彩色的提示文字
- ✅ 绿色表示成功，❌ 红色表示失败

**步骤5：完成**
- 处理完成后，按任意键关闭窗口即可
- 您的聊天记录已经更新好了！

---

### 方法二：批量处理（处理多个聊天记录）

如果您有多个聊天记录文件夹需要处理，可以用这个方法。

**步骤1：准备文件夹结构**
- 创建一个新文件夹，比如叫「微信聊天记录汇总」
- 把所有要处理的聊天记录文件夹都放进去

**步骤2：运行程序**
- 双击 `Wav_to_txt.exe`
- 在弹出的窗口里输入数字 `2` 然后按回车键

**步骤3：输入路径**
- 程序会提示：`请输入总目录路径:`
- 找到您刚才创建的「微信聊天记录汇总」文件夹
- 复制它的路径（在文件资源管理器地址栏右键复制）
- 粘贴到命令窗口里，按回车键

**步骤4：确认处理**
- 程序会列出找到的所有子目录
- 输入 `y` 然后按回车键确认开始处理
- 程序会自动依次处理每个文件夹

---

## 四、查看结果

处理完成后，怎么查看效果呢？

1. 打开聊天记录文件夹
2. 双击任意一个 `.html` 文件（会用浏览器打开）
3. 原来显示「[语音消息]」的地方，现在会显示「[语音转文字-xxx]」
4. xxx 就是语音里的文字内容！

---

## 五、常见问题解答

### Q1：运行程序后窗口一闪就没了？
- **可能原因**：程序找不到配置文件或语音文件
- **解决方法**：
- 确保 `Wav_to_txt.exe` 和 `config.ini` 在同一个文件夹里
- 确保这个文件夹里有 `.html` 文件和 `media/voices` 目录

### Q2：提示「API密钥错误」或转写失败？
- **可能原因**：配置文件里的 API 密钥不对或者过期了
- **解决方法**：
- 用记事本打开 `config.ini` 文件
- 检查 `KEY = ` 后面的内容是否正确
- 如果不确定，可以联系开发者获取新的密钥

### Q3：处理速度很慢？
- **正常情况**：语音转文字需要联网调用服务器，处理时间取决于：
- 语音文件的数量和大小
- 网络速度
- 服务器繁忙程度
- **请耐心等待**，程序会自动处理，不用管它

### Q4：程序卡住了怎么办？
- 可以按 `Ctrl + C` 组合键中断程序
- 不用担心！已经转换的结果会自动保存，下次可以继续处理

### Q5：处理完后语音文件不见了？
- 这是正常的！默认设置会在转写成功后删除原音频文件
- 如果想保留语音文件，可以修改配置文件：
- 用记事本打开 `config.ini`
- 找到 `delete_wav_after_transcribe = true`
- 把 `true` 改成 `false`
- 保存文件即可

---

## 六、重要提醒

1. **备份文件**：处理前最好备份一下聊天记录文件夹，以防万一
2. **网络连接**：处理过程需要联网，请确保网络畅通
3. **不要中断**：处理过程中尽量不要关闭窗口，否则可能影响结果
4. **文件位置**：程序必须和聊天记录文件在同一个文件夹里才能正常工作

---

## 七、联系我们

如果您在使用过程中遇到任何问题，可以联系开发者寻求帮助。

---

*祝您使用愉快！🎉*